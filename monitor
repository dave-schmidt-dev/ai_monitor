#!/bin/sh
""":" # Shell/Python polyglot — finds the project venv automatically
exec "$(dirname "$0")/.venv/bin/python3" "$0" "$@"
"""
"""Convenience wrapper for launching the monitor from the repo root."""

from ai_monitor.__main__ import main


if __name__ == "__main__":
    raise SystemExit(main())
